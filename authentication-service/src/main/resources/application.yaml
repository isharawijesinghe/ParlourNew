server:
  port: 9002

spring:
  application:
    name: AUTHENTICATION-SERVICE

eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/